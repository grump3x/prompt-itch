# Laconic

Brief prose, no AI tells. English-only codebase, answers in your language.

Pick `laconic` in `/config` → **Output style**. Applies after `/clear`.
For every project: `"outputStyle": "laconic"` in `~/.claude/settings.json`.
